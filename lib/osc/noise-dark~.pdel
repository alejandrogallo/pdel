;;; osc/noise-dark~ --- converted PDEL library definition
;;; category: oscillator
;;; note: dark filtered noise source; inlet exists for a uniform abstraction boundary

(defpdel osc/noise-dark~ ((trigger-unused control)) :outputs ((audio signal)) (let ((unused trigger-unused)) (lop~ #(3500) (noise~))))
