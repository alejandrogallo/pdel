;;; osc/square-soft~ --- converted PDEL library definition
;;; category: oscillator

(in-package #:pdel-user)

(defpdel osc/square-soft~ ((frequency-hz signal)) :outputs
  ((audio signal)) (lop~ #(9000) (osc/square~ frequency-hz)))
