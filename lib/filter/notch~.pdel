;;; filter/notch~ --- converted PDEL library definition
;;; category: filter
;;; note: fixed 1kHz notch

(in-package #:pdel-user)

(defpdel filter/notch~ ((audio signal)) :outputs ((audio signal))
  (let* ((x audio) (band (bp~ #(1000 5) x))) (-~ x band)))
