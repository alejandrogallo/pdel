;;; pd-lib-name: filter/notch~
;;; pd-lib-category: filter
;;; pd-lib-inlets: ((0 signal audio))
;;; pd-lib-outlets: ((0 signal audio))
;;; pd-lib-note: fixed 1kHz notch

(pd-lisp!
 (let* ((x (@inlet~)) (band (@bp~ [1000 5] x)))
   (@outlet~ (@-~ x band))))
