;;; drums/bongo-high~ --- converted PDEL library definition
;;; category: percussion

(in-package #:pdel-user)

(defpdel drums/bongo-high~ ((trigger control)) :outputs
  ((audio signal))
  (let* ((env (vline~ (msg #("1 1, 0 180 1") trigger))))
    (*~ (osc~ #(310)) env)))
