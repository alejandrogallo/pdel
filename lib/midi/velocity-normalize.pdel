;;; midi/velocity-normalize --- converted PDEL library definition
;;; category: midi

(in-package #:pdel-user)

(defpdel midi/velocity-normalize ((velocity-0-to-127 control))
  :outputs ((gain-0-to-1 control)) (/ #(127) velocity-0-to-127))
